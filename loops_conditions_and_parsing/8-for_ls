#!/usr/bin/env bash
# List files in current directory, show only part after first dash

for file in *; do
    # Skip hidden files (already ignored by *)
    if [[ "$file" == *-* ]]; then
        echo "${file#*-}"
    else
        echo "$file"
    fi
done
